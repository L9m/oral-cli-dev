'use strict';

const axios = require('axios')
const urljoin = require('axios')

function getNpmInfo(npmName, registry) {
    if (!npmName) {
        return null
    }
}

module.exports = {
    getNpmInfo
};
